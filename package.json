{
  "name": "kyc-platform",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build:packages && npm run build:dashboard && npm run build:websdk",
    "build:packages": "tsc --build packages/core packages/db packages/adapters packages/agent packages/api packages/worker",
    "build:dashboard": "npm run -w @kyc/dashboard build",
    "build:websdk": "npm run -w @kyc/websdk build",
    "dev:websdk": "npm run -w @kyc/websdk dev",
    "clean": "rm -rf packages/*/dist apps/*/dist packages/*/*.tsbuildinfo",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down",
    "db:generate": "npm run -w @kyc/db generate",
    "db:deploy": "npm run -w @kyc/db deploy",
    "db:migrate": "npm run -w @kyc/db migrate",
    "db:status": "npm run -w @kyc/db status",
    "db:push": "npm run -w @kyc/db push",
    "db:seed": "npm run -w @kyc/db seed",
    "bootstrap": "npm run infra:up && npm run db:generate && npm run db:deploy && npm run db:seed",
    "dev": "concurrently -n api,worker,dash -c cyan,magenta,green \"npm run -w @kyc/api dev\" \"npm run -w @kyc/worker dev\" \"npm run -w @kyc/dashboard dev\"",
    "dev:api": "npm run -w @kyc/api dev",
    "dev:worker": "npm run -w @kyc/worker dev",
    "dev:dashboard": "npm run -w @kyc/dashboard dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --build --dry packages/core packages/db packages/adapters packages/agent packages/api packages/worker && tsc --noEmit -p apps/dashboard && tsc --noEmit -p packages/websdk",
    "lint": "tsc --noEmit -p tsconfig.base.json || true"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "concurrently": "^9.1.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.4.3",
    "vitest": "^3.2.7"
  }
}
